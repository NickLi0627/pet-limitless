export interface HouseModel {
  title: string;
  description: string;
  price: string;
  imgUrl: string;
}

export interface RoommateModel {
  name: string;
  gender: string;
  age: string;
  city: string;
  photoUrl: string;
}

// export default { HouseModel, RoommateModel };
